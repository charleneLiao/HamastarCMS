<%= render partial: "/erb/sys_variable" %>

<!-- //搜尋================================================================ -->
<!-- 6種表格樣式 -->
<%#= render partial: "/erb/area-table_page-table-style", locals: set({ header_text: "6種表格樣式", func_set: "" }) %>


	<!-- page-search 內頁搜尋 -->
		<%= render partial: "/erb/area-form_page-search", locals: set({ header_text: "頁面搜尋", func_set: "{'reset':{},'toggleBtn':{'cookie':'true'}}" }) %>
	<!-- page-search 結束 -->

<!-- //步驟================================================================ -->

	<!-- step 步驟 -->
		<%= render partial: "/erb/list-text_step", locals: set({}) %>
	<!-- step 結束 -->

<!-- //分類區塊================================================================ -->

	<!-- classify 內頁列表群組 -->
		<%= render partial: "/erb/list-text_classify", locals: set({ header_text: "分類" }) %>
	<!-- classify 結束 -->

	<!-- classify 內頁列表群組 -->
		<%= render partial: "/erb/group-list_classify-multiple", locals: set({ header_text: "多重分類", inner: "/erb/list-text_classify-multiple" }) %>
	<!-- classify 結束 -->

<!-- //頁籤================================================================ -->

	<!-- tab 頁籤 -->
		<%= render partial: "/erb/group-tab_default", locals: set({ index: 9, func_set: "{'tab':{}}", header_text: "頁籤", inner: "/erb/group-list_message" }) %>
	<!-- tab 結束 -->

<!-- //按鈕================================================================ -->

	<!-- btn 按鈕 -->
		<%= render partial: "/erb/list-text_btn", locals: set({}) %>
	<!-- btn 結束 -->

	<!-- btn 按鈕 -->
		<%= render partial: "/erb/list-text_btn", locals: set({ class_set: "is-right" }) %>
	<!-- btn 結束 -->

	<!-- btn 按鈕 -->
		<%= render partial: "/erb/list-text_btn", locals: set({ class_set: "is-center" }) %>
	<!-- btn 結束 -->

	<!-- btn 按鈕 -->
		<%= render partial: "/erb/list-text_btn", locals: set({ class_set: "is-justify" }) %>

<!-- //內頁敘述群組================================================================ -->

	<!-- page-block 內頁區塊群組 -->
		<%= render partial: "/erb/group-list_page-block", locals: set({ header_text: "內頁區塊群組", inner: "/erb/area-figure_page-figure" }) %>
	<!-- page-block 結束 -->

	<!-- page-list 內頁列表群組 -->
		<%= render partial: "/erb/group-list_page-list", locals: set({ header_text: "內頁列表群組", inner: "/erb/area-figure_page-img-figcaption-p" }) %>
	<!-- page-list 結束 -->
	
	<!-- simple-result 文字回覆 -->
		<%= render partial: "/erb/simple-text_text-result", locals: set({ header_text: "得分" }) %>
	<!-- simple-result 結束 -->

	<!-- page-list 內頁列表群組 -->
		<%= render partial: "/erb/group-list_page-list", locals: set({ header_text: "問答列表", inner: "/erb/list-text_answer", header_text: lorem_word }) %>
	<!-- page-list 結束 -->

	<!-- page-list 內頁列表群組 -->
		<%= render partial: "/erb/group-list_page-list", locals: set({ header_text: "活動列表", inner: "/erb/area-essay_active" }) %>
	<!-- page-list 結束 -->

<!-- //頁面控制項================================================================ -->

	<!-- jump-tool 跳轉導航列 -->
		<%= render partial: "/erb/list-text_jump-tool", locals: set({ index: 1, header_text: "跳轉導航列" }) %>
	<!-- jump-tool 結束 -->

	<!-- pagination 客製分頁與筆數-->
		<%= render partial: "/erb/area-customize_pagination", locals: set({ index: 3, header_text: "客製分頁與筆數" }) %>
	<!-- pagination 結束 -->

	<!-- pagination 客製分頁與筆數-->
		<%= render partial: "/erb/area-customize_notitle01-pager", locals: set({ index: 3, header_text: "客製分頁與筆數" }) %>
	<!-- pagination 結束 -->